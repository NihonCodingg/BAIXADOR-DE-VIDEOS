"""Fila de trabalhos e worker em background.

Nota: este pacote se chama `queue`, mesmo nome do módulo da biblioteca padrão.
Não há conflito porque o Python 3 usa imports absolutos: dentro daqui,
`import queue` resolve para a stdlib, e este pacote é sempre `src.queue`.
"""
