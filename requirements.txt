# Dependências de produção.
# Cada uma justificada — se não dá para justificar, não entra.

# O motor de download. Usado como BIBLIOTECA (import yt_dlp), nunca por
# subprocess: o próprio projeto avisa que a saída da CLI não é contrato
# estável. Versão fixada com piso porque extractors quebram quando os sites
# mudam, e atualizar é rotina.
yt-dlp>=2026.7.4

# Servidor da API. Escolhido por validação de corpo e tipagem embutidas, o
# que mantém src/web/ fino: ele só valida e delega para o pipeline.
fastapi>=0.115

# Servidor ASGI que roda o FastAPI. Não é opcional — o FastAPI não serve
# sozinho.
uvicorn>=0.32

# Leitura de config/perfis.yaml e config/projetos.yaml. YAML e não JSON
# porque os perfis são editados à mão e comentários importam: cada seletor
# de formato tem uma justificativa que precisa morar ao lado dele.
PyYAML>=6.0

# NÃO estão aqui, de propósito:
#   sqlite3   - biblioteca padrão
#   threading - biblioteca padrão
#   ffmpeg    - binário externo, instalado pelo sistema (winget install Gyan.FFmpeg)
#   mutagen   - só seria preciso para embutir thumbnail, que ficou fora (SPEC 14.2)
#   tkinter   - biblioteca padrão; só o seletor nativo de pasta a usa, num
#               subprocesso, e a ausência dela vira erro tratado na tela
